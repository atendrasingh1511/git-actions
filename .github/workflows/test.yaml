name: Deploy website
on:
  push:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Lint code
        run: npm run lint
      - name: Test code
        run: npm run test
  build:
    needs: test
    runs-on: ubuntu-latest
    outputs:  
      script-file:  ${{ steps.publish.outputs.script-file-js }} # here script-file is arbitrary, you can use any name
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Build website
        run: npm run build
      - name: Publish Js filename
        id: publish # any id name you can take
        run: find dist/assets/*.js  -type f  -execdir echo  'script-file-js={}'  >>  $GITHUB_OUTPUT  ';' # here script-file is arbitrary, you can use any name
      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: dist-files
          path: dist
        #  path: |
        #    dist
        #    package.json  # we can provide files
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Get build artifacts
        uses: actions/download-artifact@v3
        with:
          name: dist-files
      - name: Output contents
        run:  ls
      - name: Output Filename
        run:  echo "${{ needs.build.outputs.script-file }}" # needs is the context object
      - name: Deploy
        run: echo "Deploying..."